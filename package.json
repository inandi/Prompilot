{
  "name": "prompilot-dev",
  "displayName": "Prompilot Dev",
  "description": "Manage custom instructions with a status bar button",
  "version": "3.0.0",
  "publisher": "iNandi",
  "icon": "media/logo.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/inandi/prompilot.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "prompilot-dev.showPrompts",
        "title": "Show Prompts"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  }
}